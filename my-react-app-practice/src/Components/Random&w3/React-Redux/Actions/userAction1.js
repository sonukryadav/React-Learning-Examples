

export const userAction1 = (payload, dispatch) => { 

    dispatch({
        type: "USERS",
        payload: payload,
    })

};

// export const deleteUserAction = (data, dispatch) => {
//     dispatch({
//         type:"DELETE",
//     });
// }

